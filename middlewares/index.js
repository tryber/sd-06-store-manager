const checkNameAvailability= require('./checkNameAvailability');
const handleError = require('./handleError');
const validateProduct= require('./validateProduct');

module.exports = {
  checkNameAvailability,
  handleError,
  validateProduct,
};
